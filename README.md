# Portfolio

Personal portfolio site — React, Vite, Tailwind CSS, Framer Motion.

## Local setup

```bash
npm install
npm run dev
```

## Environment (contact form)

Copy `.env.example` to `.env` and add your EmailJS keys. See `.env.example` for variable names.
